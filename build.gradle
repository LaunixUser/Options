apply plugin: 'java'

sourceCompatibility = 1.11

group 'org.ml'
version '3.2'

repositories {
    mavenCentral()
}

dependencies {
    compile (
            [group: 'org.jdom', name: 'jdom2', version: '[2.0,)']
    )
}
