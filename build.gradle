plugins {
    id 'java'
}

group 'org.ml'
version '3.1'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile (
            [group: 'org.jdom', name: 'jdom2', version: '[2.0,)']
    )
}
